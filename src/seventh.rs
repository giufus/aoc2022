use std::ops::Not;
use std::str::FromStr;
use std::string::ParseError;

use r3bl_rs_utils::Arena;

use crate::util::{get_input_as_stream, get_input_as_string};

pub fn run(path: &str) {
    println!("7th: Nothing 7th")
}

